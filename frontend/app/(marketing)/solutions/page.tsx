import Link from "next/link";
import { Footer, Navigation, PageHero } from "../components";
const items = [["Lead to project", "Keep customer details, communications, and opportunity status organized. When work is won, carry the context into the project."],["Project coordination", "Organize projects into phases and tasks, give the right people a clear view of their responsibilities, and keep progress visible."],["Documents & daily logs", "Keep versions of project documents close to the work and capture jobsite updates where the office can act on them."],["Estimates & approvals", "Create a smoother path from estimate to customer approval with connected e-signature workflows."]];
export default function Solutions() { return <><Navigation /><main><PageHero eyebrow="THE WORKFLOW" title="Every part of the job, working together." copy="Builders Stream is designed around the real handoffs of construction work—before the build, on the jobsite, and through approval." /><section className="shell solution-grid">{items.map(([title, copy], i) => <article className="solution" key={title}><span>0{i + 1}</span><h2>{title}</h2><p>{copy}</p><div className="solution-rule"></div></article>)}</section><section className="image-panel shell"><img src="/images/project-flow.png" alt="Builders Stream event flow diagram" /><div><p className="eyebrow">A BETTER HANDOFF</p><h2>Less re-entry.<br /><em>More momentum.</em></h2><p>Information should travel with the work—not get recreated every time a project reaches a new stage.</p><Link className="text-link" href="/early-access">Request early access <span>→</span></Link></div></section></main><Footer /></>; }
